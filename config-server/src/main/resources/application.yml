info:
  component: SpringConfig-Server
  
server:  port: 8888
  
spring:
  cloud:
    config:
      server:
        git:
          uri: http://github.com/javerlobo/gescoches  profiles:    active: dev